import styled from "styled-components";

export const RestaurantsCardsDiv = styled.div`
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    justify-items: center;
    gap: 20px;
    width: 100%;
    margin: 0 auto;
    padding: 20px 0;
`;